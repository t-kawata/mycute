---
ticket_id: 8
title: 音声入力ルーティング基盤 (Voice Input Router / Orchestrator)
slug: voice-input-router-orchestrator
status: draft
created_at: 2026-05-18
updated_at: 2026-05-18
---
# 音声入力ルーティング基盤 (Voice Input Router / Orchestrator)

## Summary

音声による入力をリアルタイムに受け取り、AI要約を経てオーケストレーターへ渡す汎用基盤を構築する。本チケットの本質はカスタムコマンドの作成ではなく、**音声入力の汎用ルーティング基盤**である。カスタムコマンド作成はこの基盤を利用した具体例の1つであり、別チケットで対応する。

## Background

現在の「音声入力インプット」(Alt/Option ダブルタップ) はカーソル位置への文字列入力 (バッファ&フラッシュ) に特化しており、AIとの対話やエージェントへの指示入力には使えない。今後、音声UIが主要な操作手段となることを見据え、以下の要件を満たす汎用基盤が必要である：

- 発話内容をリアルタイム文字起こし
- AI要約による発話の確定
- オーケストレーターによる処理振り分け
- 1つのオーバーレイ内で完結する対話セッション

## Scope

### 1. 音声認識エンジンの汎用化（バックエンドのみ）

既存の「音声入力インプット」と新設する「音声入力ルーティング」で、**バックエンドのリアルタイム音声認識（STT）機構**を共通化・抽象化する。

**共通化しないもの（＝既存フロントエンドには一切触れない）**：
- 音声入力インプットのトリガー（Altダブルタップ）
- 音声入力インプットのUI/UX（バッファ&フラッシュ、カーソル位置への入力）
- 音声入力インプットのフロントエンド実装（Vueコンポーネント）

### 2. 音声入力オーバーレイ（独立コンポーネント）

- キー操作で起動する全画面オーバーレイ
- **デザインは既存の「音声入力インプット」用オーバーレイ (`web/src/components/tools/OverlayView.vue`) を踏襲し、それをベースにカスタマイズする**
  - 既存オーバーレイのコンテナ構造、背景スタイル（半透明ガラス効果 `backdrop-filter: blur`）、フォント（MPLUSRounded1c）、アニメーション（scale + border-radius 遷移）を継承
  - その上で、音声入力ルーティング専用のUI要素（対話履歴表示、マークダウンレンダリング、オーケストレーター処理中アニメーション等）を追加する
- 発話内容をリアルタイムで文字起こし表示
- オーケストレーターからの応答をマークダウンレンダリングで表示
- セッション全体の会話がスクロールで閲覧可能
- **デザイン**:
  - ユーザー発話: 小さめの文字、半透明グレーの背景（ブレーン状）で判別可能に
  - AI応答: 背景なし、メインコンテンツとして大きく表示
- 処理中（オーケストレーター/エージェント処理中）は美しいアニメーションで思考状態を表示
- マークダウン→HTML変換ライブラリを選定し、AI応答を美しく表示する

### 3. キーバインド（音声入力ルーティング専用トリガー）

- **トリガー**: Ctrl+Alt（Windows）/ Control+Option（macOS）の同時押し
- **順序非依存**: どちらの修飾キーを先に押しても検出する（「片方のみ→両方」の状態遷移で判定）
- **150ms クールダウン**: 同時押しを確定させるための猶予（誤発火防止）
- 新しい HotkeyAction 種別を追加（`RouterInput` 等）
- キーバインドは設定ファイルで変更可能にする
- 既存の「音声入力インプット」(Alt/Option ダブルタップ) とは完全に独立したバインドとする

### 4. 対話モデル（Tap-to-Talk + 複数ターン）

- **セッション開始**: Ctrl+Alt 同時押し → オーバーレイ表示・録音開始
- **発話確定**: 再度 Ctrl+Alt 同時押し → 録音終了・オーケストレーターへ送信
- **複数ターン**: オーケストレーターの応答後、さらに Ctrl+Alt 同時押しで追加発話可能
- **セッション終了**: オーケストレーターがタスク完了と判断した時点で、即座に閉じるのではなく、ユーザーに「タスク完了と判断しました。他に何もなければセッションを閉じます。」旨を伝えた上で、ユーザーの応答を待つ。ユーザーが追加の発話をしなければ自然にセッション終了。ユーザーがさらに発話すれば継続する。
- セッション内の会話履歴は文脈として維持される

### 5. オーケストレーター（※実装詳細は別途議論）

**本チケットでは以下をスコープとする**：
- オーケストレーターのインターフェース（trait/type definition）の定義
- オーケストレーターへの入力値の型定義
- オーケストレーターからの出力値（応答）の型定義
- 最低限のモック/スタブ実装

**本チケットでは実装を保留し、別途詳細を詰める**：
- オーケストレーターの具体的な実装（エージェント群の管理、タスク振り分けロジック等）
- タスク完了判定ロジック
- カスタムコマンド作成フロー（別チケット）

## Non-scope

- カスタムコマンドの作成・管理機能
- カスタムコマンドの保存・再利用機能
- カスタムコマンド一覧UI
- 音声入力インプット（Altダブルタップ）の動作変更（既存機能は維持）

## Investigation

### 確認済みの既存実装

1. **ホットキー基盤** (`src/hotkey_mac.rs`, `src/hotkey_win.rs`, `src/hotkey_win_hook.rs`)
   - macOS: CGEventTap によるグローバルキーフック（Accessibility 権限必要）
   - Windows: rdev + WH_KEYBOARD_LL + GetAsyncKeyState の3経路併用
   - いずれも修飾キーの状態追跡が実装済みであり、Ctrl+Alt/Control+Optionの検出に流用可能

2. **HotkeyAction enum** (`src/types.rs`)
   - 現在: `Start`, `Correct`, `Summarize`, `BufferFlush`
   - ここに `RouterInput` を追加する

3. **HotkeyConfig** (`src/mycute_settings.rs`)
   - 現在: `correct`, `summarize`, `buffer_start`, `buffer_flush`
   - ここにルーター用のキーバインド設定を追加する

4. **音声認識エンジン** (STT モジュール)
   - 現在の「音声入力インプット」で使用中の実装の所在を確認し、抽象化対象を特定する

### 未確認（後日調査）

- 音声認識エンジンの具体的なモジュール構成・API
- Tauri の Overlay ウィンドウ機構（独立したオーバーレイウィンドウとして実装する場合）
- Vue コンポーネントとしてのオーバーレイ実装の現状

## Technical Decisions (合意済み)

| 項目 | 決定内容 |
|------|---------|
| トリガーキー | Ctrl+Alt (Win) / Control+Option (Mac) |
| キー検出方式 | 状態遷移検出（片方のみ→両方）、順序非依存 |
| クールダウン | 150ms（同時押し確定のための猶予） |
| 録音方式 | Tap-to-Talk（押下で開始、再押下で終了・送信） |
| セッション終了 | オーケストレーターが完了判断 → ユーザーに通知 → ユーザーの追加発話がなければ自然終了 |
| オーバーレイ種別 | 全画面（ボトムから開く）、チャット形式 |
| ユーザー発話表示 | 小さめ文字 + 半透明グレー背景 |
| AI応答表示 | 背景なし、メイン表示 |
| ローディング表示 | 美しいアニメーション（処理中であることを表現） |
| マークダウン | 専用のOSSライブラリでHTML変換して表示（要選定） |
| 音声認識エンジン | 既存「音声入力インプット」と**バックエンド STT 実装のみ**共通化。フロントエンドは新設。 |

## Dependencies / Open Items

- [ ] **オーケストレーターの実装詳細**: 別途議論して決定する（本チケットではインターフェース定義のみ）
  - エージェント構成、タスク振り分けロジック、タスク完了判定等
- [ ] **マークダウンレンダリングライブラリ**: 後日選定
- [ ] **音声認識エンジンのモジュール構成**: INVESTIGATION フェーズで確認

## Test Plan

本チケットの実装を検証するためのユニットテスト計画。
/plan-ticket 実行時に具体化する。

### ユニットテスト計画（案）

1. **キーバインド検出**
   - Ctrl+Alt 同時押し → RouterInput 発火（macOS/Windows 両方）
   - Ctrl のみ押下 → 発火しない
   - Alt のみ押下 → 発火しない
   - 順序逆転（Alt→Ctrl）でも発火すること
   - クールダウン150ms未満の瞬間的な同時押し → 発火しない
   - 片方のキーを離したら再度両方押すまで発火しない

2. **オーバーレイコンポーネント**
   - 起動 → 表示・録音開始
   - 発話確定 → テキスト送信・文字起こしクリア
   - 応答表示 → マークダウンが正しくレンダリングされる
   - スクロール → 会話履歴がスクロール可能
   - セッション終了 → オーバーレイが閉じる

3. **オーケストレーターインターフェース**
   - 入力値/出力値の型が正しいこと
   - モック実装が期待通り動作すること

## Boy Scout Rule — 翻訳可能性計画

- `hotkey_mac.rs` / `hotkey_win.rs` / `hotkey_win_hook.rs`: 既存のキー検出ロジックにフラグ状態遷移の判定処理を追加する際、責務ごとに関数に抽出する
- `types.rs` / `mycute_settings.rs`: HotkeyAction / HotkeyConfig の拡張時に enum/struct の意図が自明になるよう命名する
- 音声認識エンジンの抽象化時、既存コードの責務混在を解消する

## Acceptance Criteria

- [ ] Ctrl+Alt (Control+Option) の同時押しで音声入力オーバーレイが表示され録音が開始される
- [ ] 同一キーバインドの再押下で録音が終了し、オーケストレーターに送信される
- [ ] オーケストレーターからの応答がオーバーレイ内にマークダウンレンダリングで表示される
- [ ] 複数ターンの対話が可能である
- [ ] オーケストレーターがタスク完了と判断した際、ユーザーにその旨を伝え、ユーザーの追加発話がなければ自然にセッションが閉じる
- [ ] 既存の「音声入力インプット」(Altダブルタップ) は従来通り動作する
- [ ] キーバインドの押下順序に依存しない検出ができている
- [ ] 150ms クールダウンによる誤発火防止が機能している
- [ ] 翻訳可能性の検証が通っている
- [ ] 既存テストが通過している

## Notes

### オーケストレーターの実装詳細は未確定

**オーケストレーターの具体的な実装（エージェント構成、タスク振り分けロジック、タスク完了判定等）は本チケットでは未確定である。** 以下の点について別途議論が必要：

1. オーケストレーターの内部アーキテクチャ（エージェント構成、タスク振り分けロジック）
2. タスク完了判定の基準と実装方法
3. カスタムコマンド作成フローとの連携（別チケット）
4. エラー処理（音声認識失敗、AI応答タイムアウト等）
5. 複数ターンの文脈管理（session context の保持方法）

これらの議論が完了した後、別途 /plan-ticket で本チケットの実装計画を策定する。

### 成果物

- 計画: context/0008-voice-input-router-orchestrator/plan.md（未作成、/plan-ticket 承認後に作成）
- 実装サマリ: context/0008-voice-input-router-orchestrator/implementation.md（未作成、/start-ticket 実装完了後に作成）
- レビュー報告書: context/0008-voice-input-router-orchestrator/review.md（未作成、/review-ticket 全チェック通過後に作成）

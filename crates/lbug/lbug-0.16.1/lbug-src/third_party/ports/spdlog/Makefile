# spdlog port
# Fast C++ logging library

PORTNAME=	spdlog
DISTNAME=	spdlog
VERSION=	1.14.1
DISTURL=	https://github.com/gabime/spdlog/archive/refs/tags/v${VERSION}.tar.gz

SRCDIR=	spdlog-${VERSION}

# spdlog is header-only, no build commands needed
# BUILD_COMMANDS=

.include "${.CURDIR}/../bsd.port.mk"

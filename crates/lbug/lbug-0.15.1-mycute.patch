diff -ruN /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/.cargo-ok /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/.cargo-ok
--- /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/.cargo-ok	1970-01-01 09:00:00
+++ /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/.cargo-ok	2026-03-03 14:09:30
@@ -0,0 +1 @@
+{"v":1}
\ No newline at end of file
diff -ruN /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/build.rs /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/build.rs
--- /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/build.rs	2006-07-24 10:21:28
+++ /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/build.rs	2026-03-09 17:52:45
@@ -47,7 +47,6 @@
             "parquet",
             "thrift",
             "snappy",
-            "zstd",
             "miniz",
             "mbedtls",
             "brotlidec",
@@ -55,6 +54,7 @@
             "lz4",
             "roaring_bitmap",
             "simsimd",
+            "yyjson",
         ] {
             if rustversion::cfg!(since(1.82)) {
                 println!("cargo:rustc-link-lib=static:+whole-archive={lib}");
@@ -62,6 +62,9 @@
                 println!("cargo:rustc-link-lib=static={lib}");
             }
         }
+        // zstd は whole-archive にしない（zstd-sys クレートとのシンボル競合を回避）
+        // zstd-sys が提供するシンボルを優先し、lbug が必要とするシンボルも解決される
+        println!("cargo:rustc-link-lib=static=zstd");
     }
 }
 
@@ -83,10 +86,17 @@
         .define("BUILD_SHELL", "OFF")
         .define("BUILD_SINGLE_FILE_HEADER", "OFF")
         .define("AUTO_UPDATE_GRAMMAR", "OFF");
+
+    if cfg!(target_os = "macos") {
+        build.define("CMAKE_OSX_DEPLOYMENT_TARGET", "13.3");
+    } else if let Ok(target) = env::var("MACOSX_DEPLOYMENT_TARGET") {
+        build.define("CMAKE_OSX_DEPLOYMENT_TARGET", &target);
+    }
     if cfg!(windows) {
         build.generator("Ninja");
         build.cxxflag("/EHsc");
-        build.define("CMAKE_MSVC_RUNTIME_LIBRARY", "MultiThreadedDLL");
+        // /MT (Static CRT) に変更してRustやNativeAOTの/MT設定と合致させる
+        build.define("CMAKE_MSVC_RUNTIME_LIBRARY", "MultiThreaded");
         build.define("CMAKE_POLICY_DEFAULT_CMP0091", "NEW");
     }
     if let Ok(jobs) = std::env::var("NUM_JOBS") {
@@ -114,6 +124,7 @@
         "lz4",
         "roaring_bitmap",
         "simsimd",
+        "yyjson",
     ] {
         let lib_path = build_dir
             .join("build")
@@ -177,7 +188,11 @@
 
     if cfg!(windows) {
         build.flag("/std:c++20");
-        build.flag("/MD");
+        // /MT (Static CRT) に変更 (ccクレートの機能を使用して/MDとの競合を防ぐ)
+        build.static_crt(true);
+    } else if cfg!(target_os = "macos") {
+        build.flag("-std=c++2a");
+        build.flag("-mmacosx-version-min=13.3");
     } else {
         build.flag("-std=c++2a");
     }
diff -ruN /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/src/ffi.rs /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/src/ffi.rs
--- /var/folders/y6/l095q825059b0993jps0n6180000gn/T/tmp.kZDFDnbMQo/lbug-0.15.1/src/ffi.rs	2006-07-24 10:21:28
+++ /Users/kawata/shyme/mycute/crates/lbug/lbug-0.15.1/src/ffi.rs	2026-03-03 14:09:31
@@ -1,3 +1,7 @@
+#![allow(unused_attributes)]
+#![allow(unknown_lints)]
+#![allow(warnings)]
+
 #[cfg(feature = "arrow")]
 pub(crate) mod arrow;
 

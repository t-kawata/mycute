# 観察レポート: M1.5-R3 StoredInteraction → InteractionRecord<HitlPayload> 型エイリアス移行

## 1. 計装の実装状況

- 計装対象: TC-4 クロス型 JSON ラウンドトリップ（InteractionRecord<HitlPayload> ↔ StoredInteraction）
- 実装したテストコード: src/types.rs → cross_type_json_roundtrip_n1000
- 観測した統計量: ラウンドトリップ成功率（n=1000）

## 2. 観測テスト実行結果

```
=== M1.5-R3 TC-4: クロス型 JSON ラウンドトリップ n = 1000 ===
成功: 1000/1000 (100.0%)
=== 結果: PASS ===
```

## 3. 較正ループ

本チケットは較正対象の定数が存在しないため、較正ループは実施していない。

## 4. 現象の解釈

InteractionRecord<HitlPayload> と StoredInteraction は同一型であるため、シリアライズ/デシリアライズは完全に互換である。双方向ラウンドトリップ 1000/1000 成功により、全7状態（Pending, AwaitingExternal, Resolved, TimedOut, Unreachable, ChannelClosed, Aborted）において互換性が確認された。

## 5. 目的関数 J(θ) の評価

該当なし。

## 6. 次チケットへの示唆

本チケットにより M1.5-R1 の InteractionRecord<TPayload> 型と M1.5-R2 の MetadataStore Interaction API が StoredInteraction の互換性を保ったまま統合された。後続チケット（M1.5-R4〜R11）の Event Architecture 実装の基盤として機能する。

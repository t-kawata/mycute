# 観察レポート: M1.5-R4 DarviumEvent canonical envelope + DarviumEventKind + InteractionMode 型定義

## 1. 計装の実装状況

- 計装対象: DarviumEvent（10フィールド）、DarviumEventKind（13 variant）、InteractionMode（2 variant）、補助型9種（EventCausality / EventMetadata / EventSource / TransportMeta / DeliveryMode / EventVisibility / EventRetention / EventPrivacy / PiiHandlingPolicy）、subtype enum 11種
- 実装したテストコード: src/event.rs 内 #[cfg(test)] mod tests — 8テストケース
- 観測した統計量: JSONラウンドトリップ成功率（期待値100%）、型構造の一貫性（フィールド欠落ゼロ）

## 2. 観測テスト実行結果

```
TC-1 PASS: 全13 variant のトレイト実装を確認しました
TC-2 PASS: DarviumEvent 全10フィールドの設定とアクセスを確認しました
TC-3 PASS: InteractionMode の網羅的パターンマッチを確認しました
TC-4 PASS: 1000 / 1000 ラウンドトリップ成功 (成功率 100.00%)
TC-5 PASS: 全補助型のシリアライズラウンドトリップを確認しました
TC-6 PASS: EventId の UUIDv4 互換性を確認しました
TC-7 PASS: EventSource の網羅的パターンマッチを確認しました
TC-8 PASS: 型構造の計装出力を生成しました
```

## 3. 較正ループ

本チケットは純粋な型定義であり、較正すべき定数は存在しない（該当なし）。

## 4. 現象の解釈（日本語）

DarviumEvent canonical envelope の全10フィールドが RFC §12C.1 の定義通りに実装され、JSON シリアライズ/デシリアライズが完全互換であることを確認した。DarviumEventKind の13 variant はすべて Debug / Clone / PartialEq / Serialize / Deserialize を実装し、Extension(String) escape hatch を含む extensible taxonomy が成立している。

InteractionMode（OneWay / TwoWay）の直交軸はパターンマッチで網羅可能であり、補助型9種も全てのシリアライズラウンドトリップが100%成功した。

## 5. 目的関数 J(θ) の評価

- 収束速度: N/A（型定義、較不要）
- 定常誤差: N/A
- オーバーシュート: N/A
- 総合評価: 全8テスト通過、ラウンドトリップ100%、RFC §12C と無矛盾

## 6. 次チケットへの示唆

- M1.5-R5（DarviumEventBus トレイト + FakeEventBus）は本チケットの DarviumEvent / DarviumEventKind / InteractionMode を直接参照する。envelope のフィールド構成が確定したため後続チケットで再調整は不要。
- Extension(String) variant により、subtype の将来拡張が additive に可能。
- PiiHandlingPolicy は EventPrivacy のフィールド型として event.rs に定義。M1.5-R11 で共通型定義への昇格を検討する。

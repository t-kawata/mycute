# 観察レポート: M1.76-KW-WIRE-A: offer_help_probability の epsilon_remote 経由化

## 1. 計装の実装状況

- **計装対象**: offer_help_probability() の epsilon_remote 経由化、advance_help_sessions() の定数化、AllParams G3 グループ（8パラメーター）の反映
- **実装したテストコード**: simulation.rs mod tests — test_a1 〜 test_a7
- **観測した統計量**: A1-A5, A7 は固定値 assert テスト。A6 は run_simulation 経由の全パラメーター伝搬確認（固定 seed 再現性）

## 2. 観測テスト実行結果

```
cargo test test_a -- --nocapture

test result: ok. 30 passed; 0 failed; 0 ignored; 0 measured; 1291 filtered out

A1: epsilon=0 時に OFFER_HELP_BASE=0.3 を返す → PASS
A2: epsilon 増加に伴い offer_help_probability が単調増加 → PASS
A3: ADVANCE_HELP_ACCEPT_BASE 変更で accept 確率が変化 → PASS
A4: ADVANCE_HELP_SUCCESS_BASE 変更で success 確率が変化 → PASS
A5: ADVANCE_HELP_HARMFUL_BASE 変更で harmful 確率が変化 → PASS
A6: AllParams G3 値変更で HELP プロトコルの挙動が変化（run_simulation 経由）→ PASS
A7: epsilon_remote_max=0 で offer 確率が OFFER_HELP_BASE に clamp → PASS
```

## 3. 較正ループ

### 反復 1
- 仮説: 8つのHELP確率定数をconstants.rsに追加し、AllParams G3グループ経由でシミュレーションに伝搬すれば、HELPプロトコルの挙動が較正ループから制御可能になる
- 変更定数: OFFER_HELP_BASE(0.3), OFFER_HELP_BV_COEFF(0.4), ADVANCE_HELP_ACCEPT_BASE(0.5), ADVANCE_HELP_ACCEPT_BV_COEFF(0.3), ADVANCE_HELP_SUCCESS_BASE(0.6), ADVANCE_HELP_SUCCESS_BV_COEFF(0.25), ADVANCE_HELP_HARMFUL_BASE(0.15), ADVANCE_HELP_HARMFUL_BV_COEFF(0.1)
- 観測結果: 全7テスト PASS。A6 で G3 値変更による挙動変化を確認。既存1313テスト全て PASS
- 解釈: パラメーター経路が正しく確立された。epsilon_remote の各成分（base, need_coeff, benevolence_coeff）が HELP 発動確率に反映されるようになった

## 4. 現象の解釈（日本語）

offer_help_probability() に compute_benevolence_aware_remote_exploration() を組み込んだことで、epsilon_remote_base, epsilon_remote_need_coeff, epsilon_remote_benevolence_coeff の3つのパラメーターが HELP プロトコルの発動率に影響を与えるようになった。以前は 0.3 + helper_benevolence * 0.4 の固定式だったが、今後は ε_remote(child) = clip(ε₀ + a₁·need(child) - a₂·B̄_local, 0, ε_max) の計算結果が加算される。

child_need は現在 0.0 固定（WIRE-D 未実装）のため、当面の影響は epsilon_remote_base と benevolence_coeff に限定されるが、WIRE-D 完了後は child_need に応じた動的な発動率変動が期待できる。

advance_help_sessions() の accept/success/harmful 確率は 6 つのマジックナンバーから名前付き定数経由に変更された。これにより較正ループからの制御が可能になり、RFC §41C の要求（全初期条件の較正対象露出）を満たす。

## 5. 目的関数 J(θ) の評価

- 収束速度: 本チケットはパラメーター経路の確立が目的であり、J(θ) の直接的評価は未実施
- 定常誤差: 既存テストとの回帰なし
- オーバーシュート: なし
- 総合評価: パラメーター経路確立完了。後続 WIRE-D/E での較正フェーズに備えて準備が整った

## 6. 次チケットへの示唆

- WIRE-D (REMOTE_EXPLORE_* のシミュレーション実装): child_need の実装が必要。現在 0.0 固定の child_need をエージェント状態から動的に計算する仕組みをシミュレーションに追加する
- WIRE-E (残余ハードコード値の全数パラメーター化): 本チケットで修正した 7 箇所の struct リテラル（8フィールド追加）が今後も保守されることを確認する
- AllParams G3 グループの残りパラメーター（REMOTE_EXPLORATION_*）と G3 の整合性を WIRE-D で検証する必要がある

# Release v0.24.127

## Summary
Implemented M-2-3 deterministic mock clients for RetrievalPrimitive and applied consistent code formatting across the Darvium crate.

## What's Changed
- **feat:** Add `MockEmptyRetrievalPrimitive` and `MockErrorRetrievalPrimitive` with invocation counting instrumentation (M-2-3)
- **feat:** Add `MockRetrievalPrimitive` enum for unified Empty/Error dispatch
- **test:** Add 20 tests including 3 observational tests (entropy invariance, Kolmogorov complexity, latency invariance)
- **refactor:** Replace private `DummyRetrievalPrimitive` with public `MockEmptyRetrievalPrimitive` in type tests
- **style:** Apply `cargo fmt` across `llm/mod.rs`, `store/graph_store.rs`, `types.rs`

# Release v0.24.181

## Summary
M1.75-11 implements the village calibration loop harness with the objective function J_village(θ), enabling systematic parameter exploration via OFAT, grid, and Latin hypercube sampling sweeps.

## What's Changed
- **feat**: Village calibration loop harness with objective function J_village(θ) (M1.75-11)
  - New `VillageCalibrationHarness` with 3 sweep modes: one-factor-at-a-time, grid, Latin hypercube sampling
  - New `compute_village_objective()` pure function combining churn, JSD, survival, false-new, and review-load metrics
  - New calibration data types: `ParameterRange`, `SweepMode`, `VillageCalibrationConfig`, `VillageCalibrationResult`, `CalibrationReport`
  - 8 new calibration constants in `src/constants.rs` for objective weights and sweep settings
  - 12 calibration tests (C-1 through C-12) all passing
  - Ticket spec, plan, observation report, implementation summary, and review report saved

# Release v0.24.77

## Summary
Fixed compiler warnings on macOS for Windows-specific imports and unused parameters.

## What's Changed
- **Fix**: Guard `WindowsHealthPayload` import with `#[cfg(windows)]` to suppress unused import warnings on macOS
- **Fix**: Mark unused `uri` parameter in `open_windows_settings` with `_` prefix

# Release v0.24.43

## Summary
Added `make next-version` target to simplify pre-release version retrieval.

## What's Changed
- **chore**: Add `make next-version` target that reads the current version from `MYCUTE_VERSION` constant and outputs the next patch version. Useful for release note generation and other pre-push tooling.

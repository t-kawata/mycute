# Release v0.24.195

## Summary
Implement M1.76-14 perturbation test suite (SHOULD perturbation) for the Darvium Reciprocity module, verifying stability under small input perturbations across 7 tests including sigma sweep response and n=100 statistical analysis.

## What's Changed
- **feat(darvium)**: Add `PerturbationKind` enum (5 variants), `StabilityRegressionSummary`, `OscillationDetector`, `apply_perturbation`, `compute_stability_summary`, and `run_perturbation_suite` for perturbation testing
- **test(darvium)**: Add 7 perturbation tests — ranking stability under HelpSuccessAddition (T1), village churn under TrustDelta (T2), survival drift under offer replacement (T3), helper set integrity under reputation delta (T4), oscillation detection (T5), sigma sweep response curve (T6), and n=100 statistical summary (T7) — all PASS
- **docs**: Add M1.76-14 ticket spec, implementation summary, observation report, and review report

# Release v0.24.71

## Summary
Removed unreachable dead code (Windows batch file cleanup functions, tests, and template) that were left over from the abandoned batch-based cleanup approach.

## What's Changed
- **refactor**: Removed `spawn_cleanup_bat()` and `escape_bat_string()` — dead code from the abandoned Windows batch cleanup approach
- **refactor**: Removed Phase 9.5 batch file spawn block from `reset_and_exit`
- **refactor**: Removed `CommandExtSafe` import (was only used by `spawn_cleanup_bat`)
- **test**: Removed 10 tests covering batch template validation, escape functions, and path validation
- **chore**: Deleted `resources/cleanup_template.bat`

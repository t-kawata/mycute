# Release v0.24.129

## Summary
Implemented M-1.5-2 terminal state non-re-entry invariant enforcement for the SearchState machine, including runtime guard logic and terminal transition reason classification.

## What's Changed
- **feat**: Add `SearchState::transition_to()` with terminal state guard and legality check
- **feat**: Add `TerminalTransitionReason` enum (5 variants) and `can_terminate_with()` auxiliary judge
- **feat**: Add comprehensive tests (T1-T6, OTS-1/2/3) including 100,000-pulse multithreaded injection
- **refactor**: Remove unnecessary `mut` from pre-existing test variables (Boy Scout cleanup)
- **docs**: Update ticket progress (M-1.5-2: spec/plan/implementation/review)

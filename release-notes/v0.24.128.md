# Release v0.24.128

## Summary
Implemented M-1.5-1 SearchState legal transition matrix — a finite state machine for the SearchWorkflow engine with full 8×8 exhaustive transition test coverage.

## What's Changed
- feat: Add `SearchState` enum (8 variants: Init, Retrieve, Evaluate, Refine, Compose, ProposeNew, Finalize, Abort) per RFC §13.5
- feat: Add `is_legal_transition(from, to) -> bool` pure function encoding all 16 legal transitions
- test: 16 individual legal transition tests (T1-a through T1-p)
- test: 7 group illegal transition tests by category (T2-a through T2-g)
- test: Exhaustive 8×8 = 64 pair transition matrix test (T3)
- test: SearchState memory size boundary test (T4)
- obs: OTS-1 transition matrix completeness observation
- obs: OTS-2 spectral radius measurement (power iteration, ρ < 1 verified)
- obs: OTS-3 mean absorption time via Monte Carlo (10,000 trajectories)
- chore: Expose `SearchState` from `src/lib.rs`

# Release v0.24.152

## Summary
Implemented dual-store consistency commit protocol (M1.5-2) with hard retrieval exclusion gate and HNSW vector index mock (M1.5-1).

## What's Changed
- **feat:** Add `DualStoreCoordinator` with RFC §18.2 dual-store commit protocol (Pending → NeedsRepair → Committed/Quarantined)
- **feat:** Implement `ConsistencyState` / `CommitPhase` / `RepairLog` / `RepairAction` types with hard retrieval exclusion gate
- **feat:** Add `MockHnswIndex` for ANN simulation with configurable dimension (M1.5-1)
- **feat:** Add FailingGraphStore / FailingMetadataStore error injection wrappers
- **test:** Add 50 unit tests (T1-T50) and 4 observational tests (OTS-1~OTS-4) for dual-store protocol
- **refactor:** Apply code formatting (cargo fmt) across human_review_queue, trust module, and m1_1 tests

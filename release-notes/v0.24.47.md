# Release v0.24.47

## Summary
Fixed a bug where STT recognition history was never saved to the database due to premature buffer clearing in the stop sequence.

## What's Changed
- fix: Removed `self.buffer.clear()` from `MycuteManager::stop_recording()` — the buffer was being cleared before the event bridge's `Stopped` handler could read it, preventing history persistence since the feature was introduced

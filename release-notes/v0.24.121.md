# Release v0.24.121

## Summary
Added the LLMClient abstraction layer to Darvium, defining the `LLMClient` trait, `LlmSchema` enum, and `FakeLlmClient` deterministic mock implementation.

## What's Changed
- feat: add LLMClient trait with LlmSchema enum and FakeLlmClient mock implementation
  - `LLMClient` trait with `generate_structured` method (`Send + Sync`, object-safe)
  - `LlmSchema` enum (QueryDesignText, PatchOperations, SelfScore, FreeText) with RFC-referencing hint descriptions
  - `FakeLlmClient` with fixed-response mode and deterministic LCG-based malformed-probability mode
  - `returns_malformed()` helper constructor
  - `DarviumError::Llm` and `DarviumError::LlmMalformedJson` error variants
  - 51 tests passing (T1-T17 + returns_malformed + call_count_tracking)

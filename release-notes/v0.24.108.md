# Release v0.24.108

## Summary
Fixed the darvium plugin Makefile to explicitly pass commit message variable to the root `make push` target.

## What's Changed
- fix: darvium Makefile `push` target now explicitly passes `msg="$(msg)"` for reliable commit message delegation

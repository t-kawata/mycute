# Release v0.24.49

## Summary
History panel close button now closes the entire overlay. Removed redundant frosted glass background from history panel to prevent visual double-layer effect.

## What's Changed
- fix: History panel close button now closes the entire overlay instead of hiding only the history panel
- fix: Removed redundant `background` and `backdrop-filter` from history panel CSS to eliminate double-glass visual overlap

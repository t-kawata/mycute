# Release v0.24.64

## Summary
Added `install-ecc-mycute` Makefile target for initial plugin setup, unified remaining branding constants from "neco-asovi" to "mycute", and documented build environment setup procedure.

## What's Changed
- Add `install-ecc-mycute` Makefile target for initial ecc-mycute plugin installation on both macOS and Windows
- Rename remaining "neco-asovi" constants to "mycute" in generated constants (APP_NAME, data dirname, cert download path, temp dir prefix)
- Add SETUP.md with build environment setup instructions for macOS and Windows

# Release v0.24.58

## Summary
Fixed a crash on fresh database initialization where the orphaned migration cleanup routine tried to DELETE from a table that doesn't exist yet on first run.

## What's Changed
- **Bug fix**: `purge_orphaned_migration_entries` now gracefully handles the absence of the `seaql_migrations` table, which occurs when starting with a clean `~/.mycute` directory

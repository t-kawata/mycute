# Release v0.24.183

## Summary
Style and formatting cleanup across Darvium — cargo fmt applied project-wide, clippy warnings resolved, and unused imports removed.

## What's Changed
- **style**: Apply `cargo fmt` across 7 Darvium source files (calibration, lib, replay, report, pipeline, coordinator, workflow_cache)
- **fix**: Suppress clippy `too_many_arguments` warning on `VillageExperimentReport::new()`
- **fix**: Replace `push_str("\n")` with `push('\n')` per clippy `single_char_add_str`
- **fix**: Remove unused `std::path::PathBuf` import in report.rs test module

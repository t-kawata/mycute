# Release v0.24.50

## Summary
Application reset now also clears STT history records. Previously, `stt_histories` table data persisted across resets due to being omitted from the deletion list.

## What's Changed
- fix: Added `SttHistories::delete_many()` to `do_reset_db()` to ensure STT history is cleaned up on application reset

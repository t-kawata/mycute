# Release v0.24.56

## Summary
Fixed auto-migration failure caused by missing old migration files, and permanently deleted all old Harunohi job market migration source files.

## What's Changed
- fix: Add `purge_orphaned_migration_entries()` to clean seaql_migrations before `Migrator::up()`
- fix: Apply purge in all 3 migration paths (CL, RT, Server modes)
- refactor: Remove 13 orphaned migration `.rs` files from disk
- refactor: Clean up mod.rs registration of old job market migrations

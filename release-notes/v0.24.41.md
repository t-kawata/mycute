# Release v0.24.41

## Summary
Enhanced the GitHub release workflow to support pre-written release notes stored in the `release-notes/` directory.

## What's Changed
- **feat:** `scripts/release.sh` now checks for `release-notes/${TAG}.md` before creating a release. If the file exists, its contents are used as the release notes; otherwise, the previous static fallback message is used

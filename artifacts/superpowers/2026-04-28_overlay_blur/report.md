# 波紋エフェクトの共通化と適用 - 完了報告

## 概要
`LlmApp.vue` にあった波紋エフェクトを `WaterRipple.vue` として共通コンポーネント化し、`OverlayView.vue` にも適用しました。

## 実施内容
1.  **WaterRipple.vue (新規)**:
    - 波紋の HTML/JS/CSS を抽出し、カプセル化。
2.  **LlmApp.vue (修正)**:
    - 重複コードを削除し、共通コンポーネントに差し替え。
3.  **OverlayView.vue (修正)**:
    - 波紋エフェクトを背景として導入。

## 検証結果
`make check-fe` にてビルドの正常終了を確認。
音声入力画面と LLM 設定画面の両方で、統一されたエフェクトが動作することを確認しました。
